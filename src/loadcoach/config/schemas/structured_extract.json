{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "structured.extract result",
  "type": "object",
  "required": ["data"],
  "additionalProperties": false,
  "properties": {
    "data": { "type": "object" },
    "confidence": { "type": "string", "enum": ["low", "medium", "high"] }
  }
}
